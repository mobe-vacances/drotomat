<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="m2dl.mobe.vacances.challenge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Challenge">
        <activity android:name=".game_over.GameOverActivity"></activity>
        <activity android:name=".game.GameActivity" />
        <activity android:name=".scores.ScoresActivity" />
        <activity android:name=".settings.SettingsActivity" />
        <activity android:name=".credits.CreditsActivity" />
        <activity android:name=".rules.RulesActivity" />
        <activity android:name=".menu.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>